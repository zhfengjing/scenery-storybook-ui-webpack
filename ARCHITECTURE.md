# 项目架构说明

## 双构建系统架构图

```
                           你的 UI 组件库源码
                                  │
                    ┌─────────────┴─────────────┐
                    │                           │
                    ▼                           ▼
            ┌───────────────┐           ┌──────────────┐
            │ Webpack 构建  │           │ Storybook    │
            │  (生产用)     │           │  (文档用)    │
            └───────┬───────┘           └──────┬───────┘
                    │                           │
                    ▼                           ▼
            ┌───────────────┐           ┌──────────────┐
            │  dist/        │           │ storybook-   │
            │  (288 KB)     │           │ static/      │
            │               │           │ (7.3 MB)     │
            │ • index.js    │           │ • index.html │
            │ • index.d.ts  │           │ • assets/    │
            │ • types/      │           │ • stories/   │
            └───────┬───────┘           └──────┬───────┘
                    │                           │
                    ▼                           ▼
            ┌───────────────┐           ┌──────────────┐
            │  发布到 npm   │           │  部署到 Web  │
            └───────┬───────┘           └──────┬───────┘
                    │                           │
                    ▼                           ▼
         ┌────────────────────┐      ┌─────────────────────┐
         │ 其他项目使用        │      │ 浏览器访问           │
         │                    │      │                     │
         │ npm install        │      │ https://xxx.github. │
         │ @scenery-ui/       │      │ io/your-repo/       │
         │ components         │      │                     │
         │                    │      │ 查看组件文档和demo   │
         │ import { Button }  │      │                     │
         └────────────────────┘      └─────────────────────┘
```

## 实际文件大小对比

```bash
$ du -sh dist/ storybook-static/
288K    dist/              # Webpack 构建 - 轻量级库
7.3M    storybook-static/  # Storybook 构建 - 完整文档站
```

**大小差异原因：**

### dist/ (288KB) 包含：
- ✅ 打包后的组件代码
- ✅ TypeScript 类型定义
- ✅ Source maps
- ❌ 不包含 React（外部依赖）
- ❌ 不包含 UI 界面
- ❌ 不包含文档

### storybook-static/ (7.3MB) 包含：
- ✅ 所有组件代码
- ✅ React 完整运行时
- ✅ Storybook UI 框架
- ✅ 所有 stories 和文档
- ✅ 控制面板和交互工具
- ✅ 独立的 HTML 页面

## 使用场景举例

### 场景 1：用户安装你的组件库

```bash
# 用户在自己的项目中
npm install @scenery-ui/components
```

**使用的是什么？** → `dist/` (Webpack 构建产物)

```javascript
// 用户的代码
import { Button } from '@scenery-ui/components';

function App() {
  return <Button>Click me</Button>;
}
```

**优势：**
- 体积小（288KB）
- 按需引入
- 与用户项目的 React 共享实例

---

### 场景 2：展示组件文档

**使用的是什么？** → `storybook-static/` (Storybook 构建产物)

访问: `https://你的用户名.github.io/你的仓库名/`

**用户看到：**
- 📖 完整的组件文档
- 🎨 可交互的组件预览
- 🎮 实时修改 props 查看效果
- 📝 使用代码示例

**优势：**
- 无需安装即可查看
- 可视化界面
- 实时交互

---

## 开发工作流

```
1. 💻 开发阶段
   ↓
   运行: npm run storybook
   ↓
   在浏览器实时预览 (localhost:6006)
   ↓
   编写组件 + 文档
   ↓

2. ✅ 完成开发
   ↓
   运行: npm run build
   ↓
   生成 dist/ (准备发布)
   ↓

3. 📦 发布到 npm
   ↓
   运行: npm run publish:npm
   ↓
   用户可以 npm install
   ↓

4. 📚 部署文档
   ↓
   运行: npm run build-storybook
   ↓
   部署 storybook-static/ 到 GitHub Pages
   ↓
   访问者可以在线查看文档
```

## package.json 脚本解读

```json
{
  "scripts": {
    // === Webpack 相关 ===
    "build": "webpack --mode production",
    // 用途: 构建 npm 包
    // 产物: dist/ 目录
    // 时机: 发布前、CI/CD

    "dev": "webpack --mode development --watch",
    // 用途: 开发时监听构建（较少用）
    // 产物: dist/ 目录
    // 时机: 测试打包结果

    // === Storybook 相关 ===
    "storybook": "storybook dev -p 6006",
    // 用途: 开发时查看组件
    // 产物: 无（内存中运行）
    // 时机: 日常开发

    "build-storybook": "storybook build",
    // 用途: 构建文档网站
    // 产物: storybook-static/ 目录
    // 时机: 部署文档前

    // === 发布相关 ===
    "prepublishOnly": "npm run build",
    // 用途: npm publish 前自动运行
    // 确保使用最新的 dist/ 构建
  }
}
```

## 为什么大型 UI 库都这么做？

查看主流 UI 库的架构：

### Ant Design
- npm 包: https://www.npmjs.com/package/antd (~2MB)
- 文档站: https://ant.design (~大型网站)

### Material-UI
- npm 包: https://www.npmjs.com/package/@mui/material
- 文档站: https://mui.com

### Chakra UI
- npm 包: https://www.npmjs.com/package/@chakra-ui/react
- 文档站: https://chakra-ui.com

**都是双构建系统：**
- 📦 Webpack/Rollup/Vite → npm 包
- 📚 Storybook/Docusaurus → 文档站

## 关键要点总结

| 方面 | Webpack | Storybook |
|------|---------|-----------|
| **目的** | 给开发者用 | 给查看者看 |
| **产物** | 代码库 | 网站 |
| **体积** | 极小 | 较大 |
| **包含内容** | 仅组件 | 组件+文档+UI |
| **使用方式** | `import` | 浏览器访问 |
| **更新频率** | 每次发版 | 每次提交 |

## 能否合并为一个？

**技术上可以，但不推荐：**

❌ **只用 Webpack**
- 用户需要下载包才能看文档
- 无法可视化预览
- 学习成本高

❌ **只用 Storybook**
- 产物体积过大
- 包含不必要的开发代码
- 影响最终应用性能

✅ **使用双系统**
- 各司其职
- 用户体验最佳
- 工业标准实践
